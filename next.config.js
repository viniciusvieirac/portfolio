/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['sangw.in', 'localhost', 'avatars.githubusercontent.com', 'via.placeholder.com', 'images.unsplash.com']
  }
}

module.exports = nextConfig
